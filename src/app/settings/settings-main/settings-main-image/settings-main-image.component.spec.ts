/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SettingsMainImageComponent } from './settings-main-image.component';

describe('Component: SettingsMainImage', () => {
  it('should create an instance', () => {
    let component = new SettingsMainImageComponent();
    expect(component).toBeTruthy();
  });
});
